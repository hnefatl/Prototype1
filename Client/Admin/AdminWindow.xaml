<Window x:Class="Client.Admin.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Admin"
        xmlns:c="clr-namespace:Client.Converters"
        mc:Ignorable="d"
        Title="Admin Controls" Height="500" Width="600"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.Resources>
        <c:BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    
    <Grid>
        <TabControl>
            <TabItem Header="Rooms">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ListView Name="List_Rooms" ItemsSource="{Binding Rooms, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="0">
                        <ListView.View>
                            <GridView>
                                <GridView.Columns>
                                    <GridViewColumn Width="30">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding Checked}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding Value.RoomName}" Width="80" Header="Room Name"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Value.StandardSeats}" Width="90" Header="Standard Seats"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Value.SpecialSeatsType}" Width="100" Header="Special seat type"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Value.SpecialSeats}" Width="80" Header="Special seats"/>
                                </GridView.Columns>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <GroupBox Header="Selected Room" Margin="5,0,5,0" Visibility="{Binding SingleRoomSelected, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BoolToVis}}" Grid.Column="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="Room Name:" Margin="5" Grid.Row="0"/>
                            <TextBox Text="{Binding SelectedRoom.RoomName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,5,5" Width="100"  Grid.Row="0" Grid.Column="1" />
                        
                            
                        </Grid>
                    </GroupBox>
                    
                    <Grid Grid.Row="1" Grid.ColumnSpan="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Name="Button_AddRoom" Content="Add Room" Height="25" Width="75" Margin="5" Grid.Column="0"/>
                        <Button Name="Button_DeleteRoom" Content="Delete Room" IsEnabled="{Binding SelectedRooms.Count}" Height="25" Width="75" Margin="5" Grid.Column="1"/>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Periods">

            </TabItem>

            <TabItem Header="Teachers">

            </TabItem>

            <TabItem Header="Students">

            </TabItem>

            <TabItem Header="Departments">

            </TabItem>
        </TabControl>
    </Grid>
</Window>
